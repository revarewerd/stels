<configuration debug="true" scan="true" scanPeriod="10 seconds" >

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
     ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%date [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <timestamp key="bySecond" datePattern="yyyyMMdd'T'HHmmss"/>

    <appender name="OUT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${WAYRECALL_HOME}/logs/odsmosru/main-${bySecond}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>${WAYRECALL_HOME}/logs/odsmosru/main-${bySecond}.%i.log.zip</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>5</maxIndex>
        </rollingPolicy>

        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>5MB</maxFileSize>
        </triggeringPolicy>
        <encoder>
            <pattern>%date [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- <logger name="ru.sosgps.wayrecall.odsmosru.RepeatedlyODSMosruSender" level="trace"/> -->

    <logger name="ru.sosgps" level="debug"/>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="OUT_FILE" />
    </root>
</configuration>